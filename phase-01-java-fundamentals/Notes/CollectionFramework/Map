what is map?
in java Map is dictionary that holds the key -> value pair

- every value has a key
and it can't contains the duplicate.

-- one value per key
-- it maintain the order:


there will be only 1 null value
// null value is going to replace the other null value

//not thread safe

constance time performance O(1)

// internal structure of hashmap
4 component
1.key
2.value
3.bucket -> internal array
4.hash function -> it takes an input as a key and generate the hase code (numerical values)


// characteristic
-> Deterministic : The same input will always produce the same output
-> Fix output sized: input sized could be different but hasCode will be same sized(32 bit, 64 bit)


// how data store in the hash
step 1: hashing the key
-> pass the key from the hashFunction and it will generate the hash code
and this hash code determined where the key value paired will be stored

//step 2 : calculate the index
int index =  hashcode % arraySized

// step 3 : storing the data at calculated index


// catch
hash code have finite number of output,
if it get same index for the many keys , then it will create the linkedList on that particular index
and store all key->value

now in the morden hashmap
if linkedList extend the certain threshold then it will convert the particular linkedList into Red-Black Tree
 (self-balanced binary search tree)


// hashmap resizing
the default size of the array is 16, if more than 10 elements are inserted if will resized
called rehashing (16 * 0.75)

simple meaning
if array sized will increased from 12 then hash will perform rehashing.(16 * 0.75)

// time complexity
-- O(1) is the time complexity
(assuming no collision)


// only the containsValue() function have same timeComplexity O(n) and O(n)
because we have to search every value from here.


// Linked hashmap
linked hashmap has a doubly linkedList
and also the linkedhashmap preserved the order

// what is the use of LinkedHashMap
it will used to remove the least recent used items , the items which is used less time compare to other that item
will be removed from the list

/ identity hashmap
in the identity hashmap hashcode is created with the object of class not the help of class
if there is two object but the same name , it will create the different hashcode with the help of memory reference ==
where as in class level hashcode used the .equals method where it compare the value if value is equals then it will
create the same hashcode and replace the old value which is already placed on the index.


// what is navigable map?
navigable extend the sorted map
and tree map implement the navigable map


sorted map and navigable map are both interfaces

navigable map provides the navigation options like retrieving the map in reversed order


//hashtable
-- hashtable is the legacy class and replaced by the CocurrentHashMap
this is slower compare to  other because it is thread safe,


//concurrent map
it will divide into 16 segment, every 16 smaller hashmap will have its own locks
java 7 feature

When we do write operation then we need locking,
otherwise for the read operation it will not required locking.

in java 8 it has been removed sue to scaling issue
it uses compare and swap method

For example Thread A work --> x = 42
Thread work -->x to 50
it will try to replace and then comeback again

no chaning unless the resizing and collision
in concurrent map in resizing of array is incremental not one at a time


//concurrentskipList
in the concurrent skip list data will be stored in the ThreadSafe balanced tree

what is skpilist?
skiplist is the Probabilistic data-structure
that allows efficient search, insertition, deletion
in the skip list there is multiple layer of linkedList.
